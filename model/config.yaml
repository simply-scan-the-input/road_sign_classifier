# config.yaml
data_root: "./data"      # expects data/train and data/val (ImageFolder structure)
img_size: 64
batch_size: 32
lr: 0.001
weight_decay: 0.000001 #changed to float 
epochs: 30
lr_step: 15
lr_gamma: 0.5
save_every: 5

